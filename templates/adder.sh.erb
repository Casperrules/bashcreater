#!bin/bash
# if group not present, create group
grep <%=@group_name%> /etc/group
if [ $? != 0 ]
 then
   groupadd <%=@group_name%>
fi
# if user not present, add user
grep <%=@user_name%> /etc/passwd
if [ $? != 0 ]
 then
  useradd -m <%=@user_name%>
fi
